{
  "name": "tesler-workspace",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter tesler-desktop run dev",
    "build": "pnpm --filter tesler-desktop run build",
    "package": "pnpm --filter tesler-desktop run package --",
    "preview": "pnpm --filter tesler-desktop run preview",
    "version": "node scripts/update-docs.mjs && git add docs/index.html packages/tesler-desktop/package.json"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=11.0.0"
  }
}
